module sineatra.sandbox;
import std.(string,socket,thread,time,fun);
import c.arpa.inet;

alias DEBUG_BUILD = true;

template debug(T){
    void debug(T str){
        static if(DEBUG_BUILD){
            writeln("$str");
        }
    }
}

void main(string[] arg){
    auto dg = \(int x,y){
        return x+y;
    };
    writeln(string-of type-of dg);
    dg.free;
}